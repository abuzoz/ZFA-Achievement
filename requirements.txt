# ملاحظة: العربية بتشتغل بدون أي مكتبة — Tk 8.6.x على ويندوز بيشكّلها أصلًا.

# لعرض صور الإنجازات واللوجو (اختياري — البرنامج يشتغل بدونها لكن بدون أيقونات).
Pillow>=10.0

# لأيقونة الساعة (system tray) عند الإغلاق (اختياري — بدونها الإغلاق ينهي البرنامج).
pystray>=0.19

# لاستيراد الكوكي من المتصفح تلقائيًا (اختياري — للمتصفحات الأقدم/فايرفوكس).
pycryptodome>=3.19
pywin32>=306

# للاستيراد العميق من كروم v20 عبر CDP (اختياري).
websocket-client>=1.6

# لبناء نسخة .exe (اختياري — شغّل: python build_exe.py).
# pyinstaller>=6.0
